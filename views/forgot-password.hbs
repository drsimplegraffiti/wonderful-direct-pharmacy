<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    {{!--
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> --}}
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/stethoscope.png">
    <title>Wonderful Direct Pharmacy | Forgot password</title>
</head>

<body>
    <h3>Forgot password</h3>
    <form action="/forgot-password" method="PUT" id="forgot">
        <label for="email">Email</label>
        <input type="email" name="email" id="email" placeholder="Enter email"> <br>
        <input type="submit" name="submit" id="submit" value="submit" class="forgot-btn">
    </form>
{{> _footer}}
</body>

{{!-- forgot password client-side handling --}}


<script>
    const form = document.querySelector('form');

    form.addEventListener('submit', async (e) => {
        e.preventDefault();

        // get values
        const googleId = form.email.value;


        try {
            const res = await fetch('/forgot-password', {
                method: 'PUT',
                body: JSON.stringify({ googleId }),
                headers: { 'Content-Type': 'application/json' }
            });
            const data = await res.json();
            console.log(data);

        }
        catch (err) {
            console.log(err);
        }
    });
</script>
</html>